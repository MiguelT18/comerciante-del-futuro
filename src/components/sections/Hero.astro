---
import { Icon } from "astro-icon/components";
import { Image } from "astro:assets";
import ForegroundImg from "@/images/hero-foreground.png";
import PrimaryButton from "@/components/ui/PrimaryButton.astro";
---

<main
  class="relative min-h-dvh bg-[url(images/hero-background.png)] bg-cover bg-center pb-4 flex flex-col justify-between"
>
  <div class="absolute inset-0 bg-black/70"></div>

  <div>
    <nav class="relative z-10 py-4 px-4">
      <h1 class="text-md text-center font-black uppercase">LOGO</h1>
    </nav>

    <div class="relative z-10 container mx-auto text-center px-4 space-y-2">
      <h1 class="text-md font-bold">
        <span
          style={{ "box-shadow": "4px 4px 1px 1px #000000" }}
          class="bg-secondary p-1 mx-2 -rotate-6 inline-block font-bold"
        >
          Aprende hoy
        </span>
        a crear una tienda online para generar $10.000 USD/mes, incluso sin experiencia
        previa.
      </h1>

      <p class="text-text text-balance text-sm inline-block">
        Incluso aunque hoy solo dependas de tu sueldo, de tu tienda física o de
        tu propia marca que casi nadie conoce.
      </p>
    </div>
  </div>

  <form class="relative z-10 space-y-2 container mx-auto px-4" action="POST">
    <Image
      src={ForegroundImg}
      alt="john y alonso asombrados por las notificaciones de ventas en su teléfono"
      width={200}
      height={200}
      style={{ width: "clamp(12rem, 50vw, 18rem)" }}
      class="-mb-8 mx-auto"
    />

    <input
      type="email"
      placeholder="user@test.xyz"
      class="bg-white text-black placeholder:text-layout/30 p-2 rounded-md w-full text-sm"
    />
    <PrimaryButton type="submit">
      <Icon name="mdi:register" />
      Registrarme
    </PrimaryButton>
  </form>
</main>
